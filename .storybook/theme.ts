import { create } from "@storybook/theming/create";

export default create({
  base: "light",
  brandTitle: "Readspace",
  brandUrl: "https://readspace.app/",
  brandImage: "/icons/logo.svg",
  brandTarget: "_self",
});
